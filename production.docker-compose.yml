version: '3.7'
services:
  backend:
    build:
      context: .
      dockerfile: production.Dockerfile
    environment: 
      - NODE_ENV=production
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./images:/usr/src/app/images